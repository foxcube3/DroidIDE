
import org.gradle.api.Plugin
import org.gradle.api.Project

class RootProjectPlugin : Plugin<Project> {
    override fun apply(project: Project) {
        // Apply the root project plugin logic here
    }
}
